apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: cloud-demo
spec:
  type: LoadBalancer
  selector:
    app: backend-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
